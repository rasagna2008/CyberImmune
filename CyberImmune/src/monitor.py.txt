import psutil

def monitor():

    processes = []

    for p in psutil.process_iter(['pid','name','cpu_percent']):

        try:
            if p.info['cpu_percent'] > 40:
                processes.append(p.info)

        except:
            pass

    return processes