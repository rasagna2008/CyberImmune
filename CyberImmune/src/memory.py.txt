import json
from datetime import datetime

def store_attack(threat):

    data = {

        "time": str(datetime.now()),
        "process": threat['name'],
        "cpu": threat['cpu_percent']

    }

    with open("logs/attacks.json","a") as f:

        json.dump(data,f)

        f.write("\n")